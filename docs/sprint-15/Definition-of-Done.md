# Definition of Done - Sprint 15 Scheduler Engine

Sprint 15 is complete only if:

- the `packages/core/src/scheduler/` module exists
- `SchedulerEngine` exists
- `SchedulerJob` exists
- `SchedulerContext` exists
- `SchedulerTrigger` exists
- scheduler exports are centralized
- `packages/core/src/index.js` exposes the scheduler
- no real planning is implemented
- no external dependency is added
- no MongoDB access is present
- no HTTP call is present
- no OpenAI call is present
- no notification is present
- no dashboard is present
- no authentication is present
- no business logic is present
- methods contain only documented TODOs or neutral behavior
- `corepack yarn lint` passes
- `corepack yarn format` passes
- imports from `packages/core` work
- the sprint remains below 500 lines of code
